get:
  summary:  Get Furrow Cell Info
  parameters:
    - name: farmUuid
      in: path
      required: true
      schema:
        type: string
        format: uuid
    - name: farmManageUuid
      in: path
      required: true
      schema:
        type: string
        format: uuid
    - name: rowId
      in: path
      required: true
      schema:
        type: integer
        format: int32
  responses:
    '200':
      description: Furrow Cell Info
      content: 
        application/json:
          schema:
            $ref: '../schema/furrowCellInfo.yaml#/FurrowCellInfo'
post:
  summary: Create Furrow Cell Info
  parameters:
    - name: farmUuid
      in: path
      required: true
      schema:
        type: string
        format: uuid
    - name: farmManageUuid
      in: path
      required: true
      schema:
        type: string
        format: uuid
    - name: rowId
      in: path
      required: true
      schema:
        type: integer
        format: int32
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../schema/furrowCellInfo.yaml#/FurrowCellInfo' 
  responses:
    '200':
      description: OK
put:
  summary: Update Furrow Cell Info
  parameters:
    - name: farmUuid
      in: path
      required: true
      schema:
        type: string
        format: uuid
    - name: farmManageUuid
      in: path
      required: true
      schema:
        type: string
        format: uuid
    - name: rowId
      in: path
      required: true
      schema:
        type: integer
        format: int32
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../schema/furrowCellInfo.yaml#/FurrowCellInfo' 
  responses:
    '200':
      description: OK