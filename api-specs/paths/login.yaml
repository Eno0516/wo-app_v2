
  post:
    summary: Order to login
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../schema/login.yaml#/LoginOrder'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '../schema/login.yaml#/LoginUser'