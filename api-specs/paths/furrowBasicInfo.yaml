get:
  summary: Get furrow Info
  parameters:
        - name: farmUuid
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: farmManageUuid
          in: path
          required: true
          schema:
            type: string
            format: uuid
  responses:
    '200':
      description: Farm page info
      content:
        application/json:
          schema:
            $ref: '../schema/furrowBasicInfo.yaml#/FurrowBasicInfo' 