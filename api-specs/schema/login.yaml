LoginOrder:
  type: object
  properties:
    userId:
      type: string
    groupId:
      type: string
    password:
      type: string
      format: password
LoginUser:
  type: object
  properties:
    userUuid:
      type: string
      format: uuid
    groupUuid:
      type: string
      format: uuid